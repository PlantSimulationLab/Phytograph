fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
pandas>=2.1.0
numpy>=1.26.0
scipy>=1.11.0
openpyxl>=3.1.0
pydantic>=2.5.0
matplotlib>=3.7.0
phytorch-lib
prosail>=2.0.0
# Pin llvmlite to version with x86_64 wheels (0.46+ dropped Intel Mac support)
llvmlite<0.46
pytexit>=0.3.4
# PyHelios is NOT a pip dependency — it's vendored as a git submodule at
# <repo>/pyhelios and installed editable from source by scripts/build-pyhelios.mjs
# (run `git submodule update --init --recursive` first). pyyaml below is one of
# PyHelios's own runtime deps (numpy is already declared above); installing the
# submodule editable pulls it in too, but we pin it here so a plain
# `pip install -r requirements.txt` is self-sufficient.
pyyaml>=5.0.0
open3d>=0.18.0
laspy[lazrs]>=2.5.0
# Geographic -> projected (UTM) conversion for binary SBET trajectory import
# (lat/lon radians -> easting/northing; see backend-api/sbet.py). Bundles the PROJ
# C library + proj.db data dir — must be in `collectAll` in build-backend.mjs, NOT
# just hiddenimports, or the packaged app crashes on the first transform.
pyproj>=3.6
# Direct PLY parsing (ASCII + binary, all endianness) that preserves arbitrary
# scalar vertex properties — open3d drops everything but position + RGB on read.
# Used by _ply_to_las to carry intensity/reflectance/custom scalars into the
# octree as LAS extra dimensions. Bundled via `collectAll` in build-backend.mjs.
plyfile>=1.0
# E57 reader (wraps libE57Format) — used by _e57_to_las to import structured
# scan grids and recover sky/miss points from the cartesianInvalidState flag for
# LAD inversion. Prebuilt wheels for CPython on macOS/Linux/Windows; bundled via
# `collectAll` in scripts/build-backend.mjs.
pye57>=0.4.0
# Cloth Simulation Filter — ground/non-ground segmentation. SWIG C-extension
# (imports as `import CSF`); prebuilt wheels for CPython 3.9-3.12 on
# macOS/Linux/Windows. Bundled via `collectAll` in scripts/build-backend.mjs.
cloth-simulation-filter>=1.1.7
# TreeIso individual-tree segmentation deps. The TreeIso algorithm itself is
# vendored (MIT) under backend-api/vendor/treeiso/; these are its runtime deps.
# cut_pursuit_py is a pybind11 C-extension (imports as `import cut_pursuit_py`)
# with prebuilt wheels for CPython 3.10-3.13 on macOS arm64 / Linux / Windows;
# bundled via `collectAll` in scripts/build-backend.mjs. The others are pure/
# wheeled scientific deps. No PyTorch/CUDA — TreeIso is CPU-only.
cut_pursuit_py>=1.0.12
numpy-indexed>=0.3.7
scikit-image>=0.22.0
PyMaxflow>=1.3.0
# GeoTIFF writer for DEM raster export (/api/dem/export-raster). Pure-Python (no
# GDAL/rasterio — keeps the PyInstaller bundle small); writes the raster plus
# GeoTIFF georeferencing tags directly. Bundled via `collectAll` in
# scripts/build-backend.mjs.
tifffile>=2023.7.10
